<%= simple_form_for [@restaurant, @review] do |f| %>
	<%= f.input :content, placeholder: "Write your review here" %>
	<%= f.input :rating, placeholder: "Add a rating" %>
	<%= f.submit "Submit review", class: "btn btn-danger" %>
	<% end %>